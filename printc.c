#include"main.h"
/**
 * print_char - Prints a character
 *@ty: Types of arguments
 *@buf: Buffer array to prints a character
 *@fl: Flags
 *@wi: Width
 *@pre: Precision
 *@s: Size
 *Return: Chars
 */
int print_char(ty, char buf[], int fl, int wi,  int pre, int s)
{
char c_1 = arg(typ, i);
return (write_character(c, buf, fl, wi, pre, s));
}
