#include"main.h"
/**
 * print_c - prints a characters
 *@ty: Types of arguments
 *@buf: Buffer array to prints a character
 *@fl: Flags
 *@wi:Width
 *@pre: Precision
 *@s: Size
 * Return: returns <  Number of chars printed >
 */
int print_c(ty, char buf[], int fl, int wi,  int pre, int s)
{
char c_1 = arg(typ, i);
return (handle_write_char(c, buf, fl, wi, pre, s));
}
